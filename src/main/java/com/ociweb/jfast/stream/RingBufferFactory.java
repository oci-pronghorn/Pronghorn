package com.ociweb.jfast.stream;

public class RingBufferFactory {
    
    FASTRingBuffer[] buffers;
    

    //public Context buildContext() one instance for all users, readers and writers
    //has catalog, dictionaries and ringBuffers
    
}
